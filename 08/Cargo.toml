[package]
name = "boot"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.9"

[[bin]]
name = "boot"
test = true
bench = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
